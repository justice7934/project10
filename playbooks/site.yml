- hosts: all
  become: true
  roles:
    - common

# 4. WAS (FastAPI)
- hosts: was
  become: yes
  roles:
    - was

# 5. Nginx (Load Balancer)
- hosts: nginx
  become: yes
  roles:
    - nginx

# 3. Redis (상태 저장소)
- hosts: redis
  become: yes
  roles:
    - redis
